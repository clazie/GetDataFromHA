# Copilot prompt:

```text
Ich brauche ein Python Programm, was bestimmte Entitäten von Homeassistant abfragt. Das soll über die API von Homeassistant geschehen.  Der dazu benötigte API-Token steht in der Datei config.json. Dort steht auch die IP und der Port von Homeassistant. In der Datei entitys.json stehen die abzufragenden Entitäten. Die Ausgabe soll in eine JSON-Datei erfolgen mit dem Namen data.json und soll nur den Namen (entity) und den Wert (state) enthalten. Das Python Programm soll nur aus der Datei src/main.py bestehen.
```
